{
  "name": "ai-literacy-app",
  "version": "1.0.0",
  "description": "AI Literacy Training Application for HBO Students",
  "main": "server/server.js",
  "scripts": {
    "install-all": "cd server && npm install && cd ../client && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "echo 'Skipping build - this is a monorepo. Deploy server or client separately.'",
    "start": "cd server && npm start",
    "vercel-build": "node scripts/vercel-prebuild.js && cd client && npm install && npm run build"
  },
  "keywords": ["ai", "education", "learning", "gamification"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
